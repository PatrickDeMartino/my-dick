# Add this only on the server or hosting platform. Never expose it in browser code.
OPENAI_API_KEY=

# Shared secret for /admin/logins (view submitted profile handles). Pick any
# long random string. Without this set, the admin page refuses access.
ADMIN_KEY=
