import fs from 'node:fs';import ts from 'typescript';
const out=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const p=dir+'/'+e.name;if(e.isDirectory())walk(p);else if(/\.(tsx?|jsx?)$/.test(p)){const s=fs.readFileSync(p,'utf8'),sf=ts.createSourceFile(p,s,99,true);function visit(n){if(ts.isFunctionDeclaration(n)&&n.name&&/(?:new \w+\.(?:\w+Geometry|Group)|new (?:Group|Mesh))/.test(n.getText(sf)))out.push({file:p,function:n.name.text});else ts.forEachChild(n,visit);}visit(sf);}}}walk('app');fs.writeFileSync('docs/catalog/model-builders.json',JSON.stringify(out,null,2));console.log(out.map(x=>x.file+' : '+x.function).join('\n'));
